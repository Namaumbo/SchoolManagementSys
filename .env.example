


# Application environment variables.
#
# This file contains default values for the environment variables used by the
# application. These variables can be overridden by creating a .env file in
# the root directory of the application.


APP_NAME= Secondary School Management System
"""
Name of the application.

This value is used when the framework needs to place the application's name in a notification or
any other location as required by the application or its packages.
"""

APP_ENV=local
"""
Environment in which the application is running.

This value determines the "environment" your application is currently
running in. This may determine how you prefer to configure various
services the application utilizes. Set this in your ".env" file.
"""

APP_KEY=base64:/RU/TiXio2Xq+ITF1xabqq+1Sy5HoFWseI/nSjHtOGU=
"""
Application key for the encryption and decryption of sensitive data.

This value is used when the framework needs to place the application's name in a notification or
any other location as required by the application or its packages.
"""

APP_DEBUG=true
"""
Determines whether the application is in debug mode.

When the application is in debug mode, detailed error messages with
stack traces will be shown on every error that occurs within your
application. If disabled, a simple generic error page is shown.
"""

APP_URL=http://localhost
"""
URL of the application.

This URL is used by the console to properly generate URLs when using
the Artisan command line tool. You should set this to the root of
your application so that it is used when running Artisan tasks.
"""

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug


# local machine
# ////////////////////////////////////
DB_CONNECTION=mysql
DB_HOST=
DB_PORT=
DB_DATABASE=
DB_USERNAME=
DB_PASSWORD=


#docker maching 
# # /////////////////////////////////////
# DB_CONNECTION=mysql
# DB_HOST=db
# DB_PORT=3306
# DB_DATABASE=laravel_db
# DB_USERNAME=laravel_user
# DB_PASSWORD=secret

"""
Environment variables for the application.

This file contains the environment variables that are used by the
application. It is used to configure the application's behavior and
connection details.

The variables are grouped by their purpose and are documented below.
"""

BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120


# Cache configuration
# ////////////////////////////////////
MEMCACHED_HOST=127.0.0.1

# Redis configuration
# /////////////////////////////////////
REDIS_HOST=
REDIS_PASSWORD=
REDIS_PORT=6379

# Mail configuration
# /////////////////////////////////////
MAIL_MAILER=smtp
MAIL_HOST=mailhog
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS=
MAIL_FROM_NAME="${APP_NAME}"

# AWS configuration
# /////////////////////////////////////
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# Pusher configuration
# /////////////////////////////////////
PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

# Vite configuration
# /////////////////////////////////////
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

# Session configuration
# /////////////////////////////////////
SESSION_DOMAIN=http://127.0.0.1:8000/
SANCTUM_STATEFUL_DOMAIN=http://127.0.0.1:8000/
BASE_URL=
